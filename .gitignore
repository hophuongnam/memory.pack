hooks/.boot-context-*
hooks/.boot-context-*.tmp
hooks/.boot-marker-*
hooks/.replay-pid-*
hooks/.replay-error-*
hooks/.statusline-clock-*
index/search.db
index/search.db-*
# statusline-token-rate.log lives in ~/.claude/ (hooks/log-token-rate.sh writes
# it there). Ignored at repo root as a safety net against accidental relocation.
statusline-token-rate.log
__pycache__/
*.pyc

# Claude Code / MCP remote-shell — machine-local personal env (symlink + localhost), not shipped
.mcp.json
.claude/

# Per-project vibe-coding-method tag read by statusline-command.sh
# ($project_dir/vibeCodingMethod) — user/workspace-local, not engine content
vibeCodingMethod

# Superpowers brainstorming session artifacts (mockups + state)
.superpowers/
